server:
  servlet:
    context-path: /store

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/store?useUnicode=true&characterEncoding=utf8
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: se1234
  jpa:
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
  thymeleaf:
    cache: false
